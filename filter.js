/* A Filter method take each element in an array and it applies conditional statement against it. 
If the condional statement is true, the element get pushed to the output array , if the condionale stament 
false , the elemet does not get to the output array*/

// var number = [1,3,4,5,6]
// var evennumber = number.filter(x=>x%2==0)
// console.log(evennumber)

const mongoose = require('mongoose');

console.log(mongoose.version);